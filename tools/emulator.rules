rules_version = '2';
// Placeholder for the emulator to boot. The rules actually under test are
// injected at runtime by initializeTestEnvironment (reads ../firestore.rules).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} { allow read, write: if false; }
  }
}
